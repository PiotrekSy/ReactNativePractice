import { View } from 'react-native'
import styles from '../../styles'

const Card = ({ children }) => {
    <View style={styles.startGameScreenItemsWrapper}>

        {children}

    </View>
}

export default Card;